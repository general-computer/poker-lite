/**
 * Poker Lite — Application Entry Point.
 *
 * Boots the browser-based Texas Hold'em poker UI. This file is the
 * bundler entry point; esbuild resolves all imports and produces a
 * single self-contained dist/bundle.js.
 */

import './ui/app.js';
